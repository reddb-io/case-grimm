# Ask the Corpus

Each card is a question the corpus can answer because the same material exists
as text, graph, tables, metadata, and ingest history.

The point is not just to show commands. It is to make the question visible,
then expose which RedDB surface makes that question answerable.

<div id="ask-corpus-root"></div>
